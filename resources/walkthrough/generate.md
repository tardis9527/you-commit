# 生成 Commit Message

配置完成后，只需两步：

1. **暂存改动** — 使用 `git add` 或在源代码管理面板中暂存文件
2. **生成消息** — 点击输入框旁的 YouCommit 图标，或按 `Ctrl+Shift+G Ctrl+Shift+A`

AI 会分析你的 diff 内容，自动生成规范的 commit message 并填入输入框。

> 支持流式输出，生成过程中可以实时看到内容逐字填入。
